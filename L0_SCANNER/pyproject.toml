[project]
name = "team5-kisia-l0"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["ada-url", "tldextract"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["src"]