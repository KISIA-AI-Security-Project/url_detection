"""
판정 항목의 이름을 한곳에 모은다.

각 판정 함수가 `name = "IP_호스트"` 처럼 문자열을 직접 들고 있으면
(1) 오타가 나도 실행 시점까지 드러나지 않고
(2) 항목명을 바꿀 때 어디를 고쳐야 하는지 찾아다녀야 하며
(3) 어떤 항목들이 존재하는지 한눈에 볼 곳이 없다.

값은 한글이다. analysis_record의 name은 SOC 분석가가 직접 읽는 값이므로,
코드 식별자만 영문으로 두고 실제 값은 한글을 유지한다.
"""

from __future__ import annotations

# ---------------------------------------------------------------------------
# 그룹 A — 호스트 구조
# ---------------------------------------------------------------------------
GROUP_A_IP_HOST = "IP_호스트"
GROUP_A_SUSPICIOUS_TLD = "의심_TLD"
GROUP_A_FREE_ISSUED_DOMAIN = "무료_발급_도메인"
GROUP_A_NONSTANDARD_PORT = "비표준_포트"

# ---------------------------------------------------------------------------
# 그룹 B — 텍스트 트릭
# ---------------------------------------------------------------------------
GROUP_B_COMBOSQUATTING = "콤보스쿼팅"
GROUP_B_TYPOSQUATTING = "타이포스쿼팅"
GROUP_B_PUNYCODE_SPOOF = "퓨니코드_위장"

# ---------------------------------------------------------------------------
# 그룹 C — 링크·파일
# ---------------------------------------------------------------------------
GROUP_C_FILE_DOWNLOAD = "파일_다운로드"
GROUP_C_DOUBLE_EXTENSION = "이중_확장자"
GROUP_C_SHORT_URL = "단축_URL"

# ---------------------------------------------------------------------------
# 그룹 D — 파라미터
# ---------------------------------------------------------------------------
GROUP_D_XSS = "XSS"
GROUP_D_OPEN_REDIRECT = "오픈_리다이렉트"

# ---------------------------------------------------------------------------
# 그룹 E — 문자열 구조
# ---------------------------------------------------------------------------
GROUP_E_DGA_PATTERN = "DGA_패턴_의심"
GROUP_E_LONG_URL = "긴_URL"
GROUP_E_BASE64 = "Base64_인코딩"
GROUP_E_HOST_SPOOFING = "접속_호스트_교란"
GROUP_E_SUBDOMAIN_ANOMALY = "서브도메인_구조_이상"

# ---------------------------------------------------------------------------
# 그룹 F — 기타
# ---------------------------------------------------------------------------
GROUP_F_NONSTANDARD_SCHEME = "비표준_프로토콜"
